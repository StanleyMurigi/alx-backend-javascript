export default function getResponseFromAPI() {
    return new Promise((resolve, reject) => {
      // Resolve the promise for now, you can add more logic here if needed
      resolve();
    });
  }
  